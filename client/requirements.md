## Packages
framer-motion | Essential for the premium animations (scroll reveal, floating cards)
lucide-react | Icons for the UI

## Notes
API endpoints:
- POST /api/waitlist accepts { email: string }
- Validations are handled via Zod schemas shared from backend
- Primary color is Electric Indigo (#4F46E5)
- Dark mode default with soft charcoal gradients
